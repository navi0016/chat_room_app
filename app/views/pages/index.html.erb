<nav>
  <div class="login_status">
    <a class="inactive" href="#Login">Login</a>
    <a class="active" href="#Logout">Logout</a>
  </div>
    <%= image_tag  ("chatlogo.png"), :class => "logo" %>

</nav>

<% @messages.each do |message| %>
<%= message.content %>
<% end %>
